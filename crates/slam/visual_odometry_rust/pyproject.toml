[project]
name = "visual_odometry_rust"
version = "0.1.0"

[tool.uv]
cache-keys = [
  { file = "Cargo.toml" },
  { file = "src/**" },
  { file = "visual_odometry_rust/**" },
]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
features = ["extension-module"]
